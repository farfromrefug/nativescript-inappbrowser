dependencies {

    def androidXBrowser = "1.3.+"
    if (project.hasProperty('androidXBrowser')) {
        androidXBrowser = project.androidXBrowser
    }
    implementation "androidx.browser:browser:$androidXBrowser"
}